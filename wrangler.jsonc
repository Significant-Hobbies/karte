{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "linkchat",
  // Custom entry wraps OpenNext with caches.default for GET / — see worker.mjs.
  "main": "worker.mjs",
  "workers_dev": true,
  // Worker is the origin for karte.cc and origin.karte.cc. The apex uses a
  // Custom Domain so Cloudflare owns its proxied DNS record and certificate;
  // the wildcard route remains for configured *.karte.cc profile domains.
  // Custom hostnames
  // (Cloudflare for SaaS) attach to the zone via the CF API in
  // src/lib/cloudflare-domains.ts. Requires the karte.cc zone in this account
  // and secrets CLOUDFLARE_API_TOKEN + CLOUDFLARE_ZONE_ID via `wrangler secret put`.
  "routes": [
    { "pattern": "karte.cc", "custom_domain": true },
    { "pattern": "*.karte.cc/*", "zone_name": "karte.cc" },
    { "pattern": "origin.karte.cc", "custom_domain": true }
  ],
  "compatibility_date": "2024-12-30",
  "compatibility_flags": ["nodejs_compat_v2", "global_fetch_strictly_public"],
  "assets": {
    "directory": ".open-next/assets",
    "binding": "ASSETS",
    "run_worker_first": ["/", "/ai-link-in-bio", "/changelog", "/faq"]
  },
  "observability": {
    "enabled": true,
    "head_sampling_rate": 0.1
  },
  "limits": {
    "cpu_ms": 30000
  },
  // Smart Placement predates the D1 cutover. Keep the current production
  // setting until a separate performance pass has current evidence to change it.
  "placement": {
    "mode": "smart"
  },
  "analytics_engine_datasets": [
    {
      "binding": "ANALYTICS"
    }
  ],
  "vars": {
    "BETTER_AUTH_URL": "https://karte.cc",
    "NEXT_PUBLIC_APP_URL": "https://karte.cc",
    "EMAIL_FROM_ADDRESS": "noreply@karte.cc",
    "EMAIL_FROM_NAME": "Karte",
    "AGENT_AUTH_DAILY_EMAIL_CAP": "80",
    "TURNSTILE_HOSTNAMES": "karte.cc",
    "LINKCHAT_DEFAULT_AI_ENDPOINT_URL": "https://ai-gateway.sassmaker.com/v1",
    "LINKCHAT_DEFAULT_AI_MODEL": "groq/llama-3.3-70b-versatile"
  },
  "send_email": [
    {
      "name": "EMAIL"
    }
  ],
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "linkchat-auth",
      "database_id": "c1a2aab0-9b0d-418b-a2da-ae627e1da72d"
    }
  ],
  "r2_buckets": [
    {
      "binding": "IMAGES_BUCKET",
      "bucket_name": "linkchat-images"
    },
    {
      "binding": "NEXT_INC_CACHE_R2_BUCKET",
      "bucket_name": "linkchat-cache"
    }
  ],
  // Durable Object queue powering OpenNext's background revalidation of
  // ISR cache entries. The queue + the worker self-reference are required
  // for incrementalCache + queue overrides to function.
  // RATE_LIMITER_DO backs src/lib/rate-limit.ts (rate-limiter-do.mjs) so
  // sliding-window counts survive deploys; callers fail open if it errors.
  "durable_objects": {
    "bindings": [
      {
        "name": "NEXT_CACHE_DO_QUEUE",
        "class_name": "DOQueueHandler"
      },
      {
        "name": "RATE_LIMITER_DO",
        "class_name": "RateLimiterDO"
      }
    ]
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["DOQueueHandler"]
    },
    {
      "tag": "v2",
      "new_sqlite_classes": ["RateLimiterDO"]
    }
  ],
  "services": [
    {
      "binding": "RAG_SERVICE",
      "service": "knowledgebase"
    },
    {
      "binding": "WORKER_SELF_REFERENCE",
      "service": "linkchat"
    }
  ]
}
