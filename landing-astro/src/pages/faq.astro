---
import Layout from '@/layouts/Layout.astro';
import ProductFooter from '@/components/ProductFooter.astro';
import SiteHeader from '@/components/SiteHeader.astro';
import { faqs, product } from '@/lib/product';

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: { '@type': 'Answer', text: faq.answer },
  })),
};
---

<Layout
  title="Karte FAQ — Profiles, inbound, access, and company scope"
  description="Straight answers about Karte public profiles, visitor chat, owner access, current cost, agent pages, and the not-yet-shipped company workflow."
  canonicalPath="/faq"
>
  <main class="karte-page">
    <SiteHeader current="faq" />
    <section class="faq-page" aria-labelledby="faq-title">
      <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} is:inline />
      <div class="section-heading">
        <p class="section-label">FAQ · PRODUCT BOUNDARIES</p>
        <h1 id="faq-title">Questions, <em>answered plainly.</em></h1>
        <p>{product.purpose} These answers describe the product that exists now.</p>
      </div>
      <dl class="faq-ledger">
        {faqs.map((faq, index) => (
          <div>
            <dt><span>{String(index + 1).padStart(2, '0')}</span>{faq.question}</dt>
            <dd>{faq.answer}</dd>
          </div>
        ))}
      </dl>
      <div class="faq-action">
        <a class="onyx-btn-primary" href="/create">Create a page <span aria-hidden="true">→</span></a>
        <a href="/ai-link-in-bio">Read the profile comparison</a>
      </div>
    </section>
    <ProductFooter />
  </main>
</Layout>
